import React from 'react';
import NavBar from '../components/Navbar';

const MainPage = () => {
    return (
        <div>
            <NavBar />
            <h1>HOME</h1>
        </div>
    );
};

export default MainPage;
